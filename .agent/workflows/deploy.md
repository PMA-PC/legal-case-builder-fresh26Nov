---
description: Deploy all changes to GitHub
---
// turbo-all

1. Check git status
2. Add all changes
3. Commit changes with a default message "Update" (allow empty if nothing to commit)
4. Push to origin main
